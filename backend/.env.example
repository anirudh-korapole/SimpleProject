# Copy this file to .env and adjust the server/database name if needed.
# No username or password required — Windows Authentication is used.

PORT=4000

# SQL Server connection settings (Windows Authentication)
# Use "localhost" for the default instance, or "localhost\INSTANCENAME"
# e.g. "localhost\SQLEXPRESS" for a SQL Server Express named instance.
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=SimpleProjectDB
